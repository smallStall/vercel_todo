import { createTheme } from "@mui/material";

export const theme = createTheme({
  typography: {
    fontSize: 16,
    h1: {
      fontSize: 40,
      paddingBottom: 20,
    },
    h2: {
      fontSize: 30,
      paddingBottom: 10
    }
  },
});